export class Platform {
    constructor (
        public x: number,
        public y: number,
        public assetUrl: string
    ) {
        // Load sprite
    }

    update () {
        // Happens every frame
    }
}